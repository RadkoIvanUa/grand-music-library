import styled from "@emotion/styled";

export const StyledSection = styled.section({
  marginBottom: "50vh",
  h2: {
    textAlign: "center",
    fontSize: 70,
  },
});

export const SwiperText = styled.div({
  width: 300,
});

export const StyledSwiperNumber = styled.img({
  position: "absolute",
  top: "5%",
  left: "1.5%",
  // transform: "rotate(-0.07turn)",
});
